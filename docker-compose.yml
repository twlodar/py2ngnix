services:
  nginx:
    image: nginx:latest
    ports:
      - "8877:80"
    volumes:
      #- /nginx/conf.d:/etc/nginx/conf.d  # Mount Nginx config from ./nginx/conf.d   ????
      - /var/snap/docker/common/var-lib-docker/volumes/portainer_data/_data/compose/33/nginx/conf.d:/etc/nginx/conf.d
      
      
    depends_on:
      - app1
      - app2
      - app3
    networks:
      - appnet

  app1:
    build: ./app1
    networks:
      - appnet

  app2:
    build: ./app2
    networks:
      - appnet
  app3:
    build: ./app3
      args:
        BUILD_VERSION: "1.0"  # zmień za każdym raze
    networks:
      - appnet    

networks:
  appnet:
    driver: bridge
